# ============================================================================
# requirements.txt - Voice Control System Dependencies
# ============================================================================

# Core Speech Recognition
faster-whisper>=0.10.0      # Primary STT engine (Faster-Whisper)
vosk>=0.3.45                # Alternative STT engine (Vosk)
numpy>=1.21.0               # Numerical processing
pyaudio>=0.2.11             # Audio I/O

# Text-to-Speech
pyttsx3>=2.90               # Offline TTS engine

# GUI Framework
# tkinter - Usually bundled with Python

# Utilities
requests>=2.25.0            # HTTP requests for model downloads

# Build Tools
PyInstaller>=5.0.0          # Executable packaging

# Optional: Enhanced Audio Processing
soundfile>=0.10.0           # Audio file I/O

# Optional: GPU Support (uncomment if needed)
# torch>=2.0.0
# torchaudio>=2.0.0

# System Dependencies:
# Windows: Microsoft Visual C++ Redistributable required
# Linux: sudo apt-get install espeak espeak-data libespeak1 libespeak-dev portaudio19-dev python3-dev
# macOS: brew install portaudio espeak