# ============================================================================
# ============================================================================
# Voice Control System v2 - Requirements
# ============================================================================
# Install with: pip install -r requirements.txt
#
# IMPORTANT: After installing pywin32, run:
#   python Scripts/pywin32_postinstall.py -install
# (Run as Administrator if needed)
# ============================================================================

# Core Speech Recognition
faster-whisper>=0.10.0      # Primary STT engine (Faster-Whisper) - REQUIRED
numpy>=1.21.0               # Numerical processing - REQUIRED
pyaudio>=0.2.11             # Audio I/O - REQUIRED

# Windows TTS (v2 - CRITICAL)
pywin32>=305                # Windows COM/SAPI support - REQUIRED FOR TTS
                            # IMPORTANT: Run post-install script after installing:
                            #   python Scripts/pywin32_postinstall.py -install

# GUI Framework
# tkinter - Usually bundled with Python

# Utilities
requests>=2.25.0            # HTTP requests for model downloads

# Build Tools
PyInstaller>=5.0.0          # Executable packaging

# Optional: Enhanced Audio Processing
soundfile>=0.10.0           # Audio file I/O

# Optional: GPU Support (uncomment if needed)
# torch>=2.0.0
# torchaudio>=2.0.0

# System Dependencies:
# Windows: Microsoft Visual C++ Redistributable required
# Linux: sudo apt-get install espeak espeak-data libespeak1 libespeak-dev portaudio19-dev python3-dev
# macOS: brew install portaudio espeak